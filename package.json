{
  "name": "wildglobalization",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "migrate": "node scripts/migrate-wordpress.mjs",
    "build": "eleventy",
    "serve": "eleventy --serve --port=8080",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.5"
  }
}
